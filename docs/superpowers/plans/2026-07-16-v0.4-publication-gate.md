# Agent Society Loop v0.4 Publication Gate Implementation Plan

1. Add durable publication domain and SQLite records with monotonic state
   transitions and payload identity checks.
2. Add a fixed Git workspace adapter for branch, status, staging, commit, push,
   and current-head operations.
3. Add a publication tool that enforces branch, ownership, current verification,
   exact approval payload, and idempotent recovery.
4. Add a GitHub REST pull-request client with existing-PR lookup and secret-safe
   errors.
5. Expose opt-in publication configuration through maintenance composition and
   CLI without changing read-only or local-apply defaults.
6. Test against temporary Git repositories and a local bare remote, then run the
   full release verification and Python version matrix.
