# Seed Society v0.5 Implementation Plan

1. Implement a bounded synchronous MCP stdio transport and tool adapter using
   the stable 2025-11-25 handshake and tool methods.
2. Prove discovery pagination, invocation, protocol errors, timeouts, namespace
   collisions, and mandatory local risk mapping with a real fake stdio server.
3. Add immutable benchmark, case outcome, evaluation decision, and deployment
   records to SQLite.
4. Implement deterministic champion/challenger aggregate gates and regression
   explanations over injected candidate runners and evaluators.
5. Add explicit promotion and make engine routing honor the active champion,
   blocking when the approved deployment cannot run.
6. Add CLI benchmark evaluation/promotion inspection, bilingual documentation,
   full tests, package smoke tests, PR matrix, and release artifacts.
